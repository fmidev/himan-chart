{
  "$schema": "http://json-schema.org/schema#",
  "type": "object",
  "required": [
    "environment",
    "cloud",
    "image",
    "job",
    "s3",
    "radon"
  ],
  "properties": {
    "environment": {
      "type": "string",
      "pattern": "^(dev|test|preop|prod)$"
    }
  }
}
